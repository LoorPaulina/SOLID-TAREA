package ocp_solucion;

public class Crepe implements Shown{
	 String isShown(SpinnerHelados crepe) {
		if(crepe.isShown()) {
			return "Creppe, "+spinnerCreppes.getSelectedItem().toString()
					+" de "+spinnerHelados.getSelectedItem().toString()
    				+", "+cant;
	}
}}


